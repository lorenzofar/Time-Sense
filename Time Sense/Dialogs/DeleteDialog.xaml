<ContentDialog
    x:Class="Time_Sense.DeleteDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Time_Sense"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="delete_dialog"
    Title="DELETING DATA">

    <StackPanel Margin="0,12">
        <TextBlock x:Name="delete_dialog_helper" x:Uid="progress_dialog_helper" TextWrapping="Wrap" Text="This process will take a long, don't close the app or lock the screen" Style="{StaticResource CaptionTextBlockStyle}"/>
        <ProgressBar Margin="0,12,0,3" HorizontalAlignment="Stretch" x:Name="bar" RelativePanel.Below="delete_dialog_helper"  RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignLeftWithPanel="True"/>
        <TextBlock TextWrapping="Wrap" x:Name="delete_progress" Margin="0,3"/>
    </StackPanel>
</ContentDialog>
